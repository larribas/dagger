apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: hello-world-
  namespace: my-namespace
spec:
  entrypoint: main
  serviceAccountName: my-namespace-workflows
  templates:
  - name: say_hello_world
    container:
      image: my-image
      command: [cowsay]
